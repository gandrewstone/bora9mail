bitcoin -server -listen -debug
namecoin -daemon -server


rm .meteor/local/db/mongod.lock


7z a -tzip <outfile>.zip client public server README *.js *.css
7z a -tzip -p<pw> -mem=AES256 <outfile>zip <infiles>
